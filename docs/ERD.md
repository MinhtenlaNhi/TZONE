# ERD (Draft)

Core collections:

- `users`: student, instructor, admin
- `courses`: course info, price, thumbnail, lessons
- `enrollments`: relation between users and courses
- `orders`: payment and transaction info
